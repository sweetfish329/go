version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: backend/Containerfile
    container_name: kifu-backend
    ports:
      - "8822:8080"
    environment:
      PORT: 8080
      DB_PATH: /data/kifu.db
    volumes:
      - kifu-db-data:/data

volumes:
  kifu-db-data:
